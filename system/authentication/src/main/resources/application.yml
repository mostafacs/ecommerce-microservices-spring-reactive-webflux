server:
  port: 8087

spring:
  application:
    name: authentication-service

keystore:
  password: admin123
  keyPairAlias: ecommerce

eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_SERVER_URL}

logging:
  level:
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate=debug: